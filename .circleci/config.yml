version: 2
jobs:
  build:
    docker:
      - image: cimg/go:1.24

    steps:
      - checkout
      - run: go install honnef.co/go/tools/cmd/staticcheck@latest
      - run: go test -v ./...
      - run: go vet -v ./...
      - run: staticcheck ./...
